async def planejar(state: dict) -> dict:
    # TODO Fase 0: decompõe a consulta em etapas de coleta
    # Entrada: state["query"], state["classification"]
    # Saída: state["plan"] = { "etapas": [...] }
    state["plan"] = {"etapas": []}
    return state
