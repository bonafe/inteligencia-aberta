async def coletar(state: dict) -> dict:
    # TODO Fase 0: executa cada etapa do plano via ferramentas MCP
    # Entrada: state["plan"]
    # Saída: state["collected_data"] = [artefatos]
    state["collected_data"] = []
    return state
